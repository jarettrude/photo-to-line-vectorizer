#!/bin/bash
# Pre-commit hook that runs all checks

set -e

echo "🔍 Running pre-commit checks..."

echo ""
echo "🧰 Running pre-commit (uv) for all staged files..."
(
  cd backend && uv run -q pre-commit run -c ../.pre-commit-config.yaml --all-files
) || { echo "❌ pre-commit checks failed"; exit 1; }

echo ""
echo "✅ All checks passed! Ready to commit."
